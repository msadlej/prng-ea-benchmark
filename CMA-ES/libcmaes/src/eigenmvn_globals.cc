/**
 * Global variables for eigenmvn random number generation
 */

#include "Random-Reader/random_reader.h"

// Define the global variables that are declared as extern in eigenmvn.h
bool g_file_mode = false;
RandomReader *random_reader = nullptr;
